const KEY='restoflow-data-v1';
const demoRows=[
['2026-08-10','Vendas balcão','Vendas',1840,'Pix'],['2026-08-10','Compra hortifruti','Ingredientes',620,'Pix'],['2026-08-11','Delivery','Delivery',2360,'iFood/Delivery'],['2026-08-11','Gás cozinha','Gás',180,'Dinheiro'],['2026-08-12','Vendas salão','Salão',3120,'Crédito'],['2026-08-12','Bebidas fornecedor','Bebidas',740,'Débito'],['2026-08-13','Vendas balcão','Vendas',2670,'Pix'],['2026-08-13','Equipe diária','Funcionários',950,'Pix'],['2026-08-14','Sexta delivery','Delivery',4210,'iFood/Delivery'],['2026-08-14','Energia','Energia',430,'Débito'],['2026-08-15','Evento aniversário','Eventos',4980,'Crédito'],['2026-08-15','Ingredientes','Ingredientes',1120,'Pix'],['2026-08-16','Vendas domingo','Vendas',3280,'Dinheiro'],['2026-08-16','Aluguel','Aluguel',1800,'Pix'],['2026-08-17','Take away','Take away',1980,'Pix'],['2026-08-17','Internet','Internet',120,'Débito'],['2026-08-18','Delivery','Delivery',2910,'iFood/Delivery'],['2026-08-18','Marketing','Marketing',350,'Crédito'],['2026-08-19','Salão','Salão',3440,'Crédito'],['2026-08-19','Limpeza','Limpeza',210,'Pix'],['2026-08-20','Vendas','Vendas',2890,'Pix'],['2026-08-20','Bebidas','Bebidas',680,'Débito'],['2026-08-21','Sexta delivery','Delivery',4560,'iFood/Delivery'],['2026-08-21','Funcionários','Funcionários',1100,'Pix'],['2026-08-22','Evento','Eventos',5350,'Crédito'],['2026-08-22','Ingredientes','Ingredientes',1250,'Pix'],['2026-08-23','Domingo','Salão',3650,'Dinheiro'],['2026-08-23','Água','Água',145,'Pix'],['2026-08-24','Vendas','Vendas',2210,'Pix'],['2026-08-24','Manutenção','Manutenção',390,'Crédito'],['2026-08-25','Delivery','Delivery',3060,'iFood/Delivery'],['2026-08-25','Ingredientes','Ingredientes',770,'Pix'],['2026-08-26','Salão','Salão',3780,'Crédito'],['2026-08-26','Impostos','Impostos',460,'Pix'],['2026-08-27','Vendas','Vendas',2940,'Pix'],['2026-08-27','Bebidas','Bebidas',810,'Débito'],['2026-08-28','Sexta','Delivery',4780,'iFood/Delivery'],['2026-08-28','Funcionários','Funcionários',1150,'Pix'],['2026-08-29','Evento','Eventos',5920,'Crédito'],['2026-08-29','Equipamentos','Equipamentos',520,'Pix'],['2026-08-30','Domingo salão','Salão',3970,'Dinheiro'],['2026-08-30','Ingredientes','Ingredientes',930,'Pix'],['2026-08-31','Take away','Take away',2140,'Pix'],['2026-08-31','Aluguel','Aluguel',1800,'Pix']
];
const expenseCats=['Ingredientes','Bebidas','Funcionários','Aluguel','Energia','Água','Gás','Internet','Marketing','Delivery','Manutenção','Impostos','Equipamentos','Limpeza','Outros'];
const revenueCats=['Vendas','Delivery','Salão','Take away','Eventos','Outros'];
const payments=['Dinheiro','Pix','Débito','Crédito','iFood/Delivery','Outros'];
let state={page:'dashboard',period:'30',typeFilter:'all',category:'all',payment:'all',search:'',sort:'recent',data:load()};
function load(){try{const x=JSON.parse(localStorage.getItem(KEY));if(x?.rows)return x}catch{}return {demo:true,rows:demoRows.map((r,i)=>({id:'d'+i,date:r[0],description:r[1],category:r[2],value:r[3],payment:r[4],note:'Dados fictícios de demonstração',type:['Ingredientes','Bebidas','Funcionários','Aluguel','Energia','Água','Gás','Internet','Marketing','Manutenção','Impostos','Equipamentos','Limpeza'].includes(r[2])?'expense':'revenue'}))}};
function save(){localStorage.setItem(KEY,JSON.stringify(state.data))}
function money(v){return new Intl.NumberFormat('pt-BR',{style:'currency',currency:'BRL'}).format(v||0)}
function esc(s=''){return String(s).replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]))}
function rows(){let r=[...state.data.rows];const now=new Date('2026-08-31T12:00:00');let start=new Date(now);if(state.period==='7')start.setDate(now.getDate()-6);if(state.period==='30')start.setDate(now.getDate()-29);if(state.period==='month')start=new Date(now.getFullYear(),now.getMonth(),1);if(state.period==='prev')start=new Date(now.getFullYear(),now.getMonth()-1,1);if(state.period==='today')start=now;if(state.period==='yesterday'){start.setDate(now.getDate()-1);now.setDate(now.getDate()-1)}if(state.period!=='all')r=r.filter(x=>new Date(x.date)>=start&&new Date(x.date)<=now);return r}
function totals(r=rows()){const revenue=r.filter(x=>x.type==='revenue').reduce((a,x)=>a+x.value,0),expense=r.filter(x=>x.type==='expense').reduce((a,x)=>a+x.value,0);return {revenue,expense,profit:revenue-expense,margin:revenue?((revenue-expense)/revenue)*100:0,count:r.length}}
function periodButtons(){return `<div class="periods">${[['7','7 dias'],['30','30 dias'],['month','Este mês'],['prev','Mês anterior'],['all','Tudo']].map(([v,l])=>`<button class="${state.period===v?'active':''}" data-period="${v}">${l}</button>`).join('')}</div>`}
function layoutHead(title,desc,button=''){return `<div class="page-head"><div><p class="eyebrow">RESTOFLOW FINANCE</p><h1>${title}</h1><p>${desc}</p></div>${button}</div>`}
function dashboard(){const r=rows(),t=totals(r),days=[...new Set(r.map(x=>x.date))].sort();return `${layoutHead('Olá, Pietro 👋','Aqui está o resumo financeiro do seu restaurante.',`<button class="btn primary" data-add="revenue">＋ Adicionar faturamento</button>`)}<div class="period-bar">${periodButtons()}<span class="date-label">10 — 31 Ago 2026</span></div><div class="kpis"><div class="card kpi"><span class="label">Faturamento</span><div class="value">${money(t.revenue)}</div><span class="delta positive">↑ 12,4% vs. período anterior</span></div><div class="card kpi"><span class="label">Despesas</span><div class="value">${money(t.expense)}</div><span class="delta negative">↑ 4,8% vs. período anterior</span></div><div class="card kpi"><span class="label">Lucro líquido</span><div class="value">${money(t.profit)}</div><span class="delta positive">↑ 18,7% vs. período anterior</span></div><div class="card kpi"><span class="label">Margem de lucro</span><div class="value">${t.margin.toFixed(1)}%</div><span class="delta positive">+2,1 p.p.</span></div><div class="card kpi"><span class="label">Lançamentos</span><div class="value">${t.count}</div><span class="delta neutral">receitas + despesas</span></div></div><div class="grid-2"><div class="card panel"><div class="panel-head"><h3>Faturamento x Despesas</h3><small>Por dia</small></div><div class="chart-box"><canvas id="mainChart"></canvas></div></div><div class="card panel"><div class="panel-head"><h3>Acesso rápido</h3></div><div class="quick"><button data-add="revenue"><b>＋ Receita</b><span>Registrar entrada</span></button><button data-add="expense"><b>− Despesa</b><span>Registrar gasto</span></button><button data-page="fechamento"><b>◷ Fechar dia</b><span>Conferir resultado</span></button><button data-page="relatorios"><b>▤ Relatório</b><span>Exportar dados</span></button></div><div class="panel-head" style="margin-top:25px"><h3>Insights</h3></div><div class="insights"><div class="insight"><strong>🏆 Melhor dia</strong>${days.length?formatDate(days[days.length-1]):'—'} teve ${money(Math.max(...days.map(d=>totals(r.filter(x=>x.date===d)).profit),0))} de lucro.</div><div class="insight"><strong>📈 Performance</strong>Seu lucro médio por dia movimentado é ${money(days.length?t.profit/days.length:0)}.</div></div></div></div><div class="grid-2" style="margin-top:18px"><div class="card panel"><div class="panel-head"><h3>Distribuição das despesas</h3><small>Por categoria</small></div><div class="chart-box sm"><canvas id="donutChart"></canvas></div></div><div class="card panel"><div class="panel-head"><h3>Fontes de faturamento</h3><small>Por categoria</small></div><div class="chart-box sm"><canvas id="revenueChart"></canvas></div></div></div>`}
function formatDate(d){return new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'short'}).format(new Date(d+'T12:00:00'))}
function tablePage(type){const isRev=type==='revenue',data=state.data.rows.filter(x=>x.type===type).sort((a,b)=>b.date.localeCompare(a.date));const total=totals(data);return `${layoutHead(isRev?'Receitas':'Despesas',isRev?'Acompanhe todas as entradas do restaurante.':'Controle seus custos e mantenha as despesas sob controle.',`<button class="btn primary" data-add="${type}">＋ Adicionar ${isRev?'faturamento':'despesa'}</button>`)}<div class="card panel table-card"><div class="toolbar"><input id="tableSearch" placeholder="Buscar por descrição..." value="${esc(state.search)}"><select id="tableCategory"><option value="all">Todas as categorias</option>${(isRev?revenueCats:expenseCats).map(c=>`<option ${state.category===c?'selected':''}>${c}</option>`).join('')}</select><select id="tablePayment"><option value="all">Todos os pagamentos</option>${payments.map(c=>`<option ${state.payment===c?'selected':''}>${c}</option>`).join('')}</select><select id="tableSort"><option value="recent">Mais recentes</option><option value="oldest">Mais antigos</option><option value="high">Maior valor</option><option value="low">Menor valor</option></select></div><div class="notice">Total no histórico: <b>${money(total.revenue+total.expense)}</b> em ${data.length} lançamentos.</div>${transactionTable(data,isRev)}</div>`}
function transactionTable(data,isRev){let f=data.filter(x=>(!state.search||x.description.toLowerCase().includes(state.search.toLowerCase()))&&(state.category==='all'||x.category===state.category)&&(state.payment==='all'||x.payment===state.payment));if(state.sort==='oldest')f.sort((a,b)=>a.date.localeCompare(b.date));if(state.sort==='high')f.sort((a,b)=>b.value-a.value);if(state.sort==='low')f.sort((a,b)=>a.value-b.value);return `<div class="table-wrap"><table><thead><tr><th>Data</th><th>Descrição</th><th>Categoria</th><th>Pagamento</th><th>Valor</th><th></th></tr></thead><tbody>${f.length?f.map(x=>`<tr><td>${formatDate(x.date)}</td><td><strong>${esc(x.description)}</strong></td><td><span class="pill gray">${esc(x.category)}</span></td><td>${esc(x.payment)}</td><td class="${isRev?'positive':'negative'}"><strong>${isRev?'+':'−'} ${money(x.value)}</strong></td><td><button class="action" data-edit="${x.id}">✎</button><button class="action" data-delete="${x.id}">⌫</button></td></tr>`).join(''):`<tr><td colspan="6"><div class="empty">Nenhum lançamento encontrado.</div></td></tr>`}</tbody></table></div>`}
function closing(){const dates=[...new Set(state.data.rows.map(x=>x.date))].sort().reverse();const date=state.closeDate||dates[0]||'2026-08-31',r=state.data.rows.filter(x=>x.date===date),t=totals(r);const by=(type)=>Object.entries(r.filter(x=>x.type===type).reduce((a,x)=>(a[x.category]=(a[x.category]||0)+x.value,a),{})).sort((a,b)=>b[1]-a[1]);return `${layoutHead('Fechamento do dia','Veja exatamente como foi o resultado de uma data.',`<input class="field" type="date" id="closeDate" value="${date}">`)}<div class="kpis"><div class="card kpi"><span class="label">Faturamento</span><div class="value">${money(t.revenue)}</div></div><div class="card kpi"><span class="label">Despesas</span><div class="value">${money(t.expense)}</div></div><div class="card kpi"><span class="label">Resultado</span><div class="value ${t.profit>=0?'positive':'negative'}">${money(t.profit)}</div></div><div class="card kpi"><span class="label">Margem</span><div class="value">${t.margin.toFixed(1)}%</div></div><div class="card kpi"><span class="label">Lançamentos</span><div class="value">${t.count}</div></div></div><div class="card panel" style="margin-bottom:18px"><div class="panel-head"><h3>RESULTADO DO DIA</h3></div><div style="font:800 28px Manrope" class="${t.profit>=0?'positive':'negative'}">${t.profit>=0?'✓ LUCRO':'! PREJUÍZO'} · ${money(Math.abs(t.profit))}</div></div><div class="grid-2 equal"><div class="card panel"><div class="panel-head"><h3>Receitas por categoria</h3></div>${by('revenue').map(([c,v])=>`<p style="font-size:12px;display:flex;justify-content:space-between"><span>${c}</span><b>${money(v)}</b></p>`).join('')||'<div class="empty">Sem receitas.</div>'}</div><div class="card panel"><div class="panel-head"><h3>Despesas por categoria</h3></div>${by('expense').map(([c,v])=>`<p style="font-size:12px;display:flex;justify-content:space-between"><span>${c}</span><b>${money(v)}</b></p>`).join('')||'<div class="empty">Sem despesas.</div>'}</div></div>`}
function analytics(){const r=state.data.rows,t=totals(r),byDay={};r.forEach(x=>{byDay[x.date]??={revenue:0,expense:0};byDay[x.date][x.type==='revenue'?'revenue':'expense']+=x.value});const best=Object.entries(byDay).map(([d,v])=>[d,v.revenue-v.expense]).sort((a,b)=>b[1]-a[1])[0];return `${layoutHead('Análises','Insights automáticos baseados nos seus lançamentos.') }<div class="grid-2 equal"><div class="card panel"><div class="panel-head"><h3>Evolução do lucro</h3></div><div class="chart-box"><canvas id="profitChart"></canvas></div></div><div class="card panel"><div class="panel-head"><h3>O que os dados dizem</h3></div><div class="insights"><div class="insight"><strong>💰 Lucro acumulado</strong>${money(t.profit)} no período registrado.</div><div class="insight"><strong>🏆 Melhor dia</strong>${best?formatDate(best[0])+' com '+money(best[1]):'Sem dados suficientes.'}</div><div class="insight"><strong>📊 Ticket médio</strong>${money(t.revenue/(r.filter(x=>x.type==='revenue').length||1))} por lançamento de receita.</div><div class="insight"><strong>🎯 Ponto de atenção</strong>Despesas representam ${t.revenue?(t.expense/t.revenue*100).toFixed(1):0}% do faturamento.</div></div></div></div>`}
function history(){return `${layoutHead('Histórico','Todas as movimentações em uma única visão.',`<button class="btn" id="exportCsv">↓ Exportar CSV</button>`)}<div class="card panel table-card"><div class="toolbar"><input id="historySearch" placeholder="Pesquisar..." value="${esc(state.search)}"><select id="historyType"><option value="all">Receitas + despesas</option><option value="revenue">Receitas</option><option value="expense">Despesas</option></select><select id="tableSort"><option value="recent">Mais recentes</option><option value="oldest">Mais antigos</option><option value="high">Maior valor</option><option value="low">Menor valor</option></select></div>${transactionTable(state.data.rows,false)}</div>`}
function calendar(){const y=2026,m=7,first=new Date(y,m,1).getDay(),days=new Date(y,m+1,0).getDate();let html='';for(let i=0;i<first;i++)html+='<div></div>';for(let d=1;d<=days;d++){const date=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`,t=totals(state.data.rows.filter(x=>x.date===date));html+=`<div class="day" data-close-date="${date}"><b>${d}</b>${t.count?`<small class="positive">↑ ${money(t.revenue)}</small><small class="negative">↓ ${money(t.expense)}</small><div class="profit ${t.profit>=0?'positive':'negative'}">${money(t.profit)}</div>`:'<small>Sem movimento</small>'}</div>`}return `${layoutHead('Calendário financeiro','Uma visão rápida do desempenho de cada dia.') }<div class="card panel"><div class="panel-head"><h3>Agosto 2026</h3><small>clique em um dia para fechar</small></div><div class="calendar">${['Dom','Seg','Ter','Qua','Qui','Sex','Sáb'].map(x=>`<div class="cal-head">${x}</div>`).join('')}${html}</div></div>`}
function reports(){const t=totals(state.data.rows),exp=state.data.rows.filter(x=>x.type==='expense').sort((a,b)=>b.value-a.value).slice(0,5),rev=state.data.rows.filter(x=>x.type==='revenue').sort((a,b)=>b.value-a.value).slice(0,5);return `${layoutHead('Relatórios','Resumo pronto para análise ou exportação.',`<button class="btn primary" id="exportCsv">↓ Exportar CSV</button>`)}<div class="report-cards"><div class="card kpi"><span class="label">Receitas</span><div class="value">${money(t.revenue)}</div></div><div class="card kpi"><span class="label">Despesas</span><div class="value">${money(t.expense)}</div></div><div class="card kpi"><span class="label">Lucro</span><div class="value">${money(t.profit)}</div></div><div class="card kpi"><span class="label">Margem</span><div class="value">${t.margin.toFixed(1)}%</div></div></div><div class="grid-2 equal"><div class="card panel"><h3>Maiores despesas</h3>${exp.map(x=>`<p style="display:flex;justify-content:space-between;font-size:11px"><span>${x.description}</span><b class="negative">${money(x.value)}</b></p>`).join('')}</div><div class="card panel"><h3>Principais fontes de receita</h3>${rev.map(x=>`<p style="display:flex;justify-content:space-between;font-size:11px"><span>${x.description}</span><b class="positive">${money(x.value)}</b></p>`).join('')}</div></div>`}
function settings(){return `${layoutHead('Configurações','Personalize o restaurante e as preferências do sistema.') }<div class="settings-grid"><div class="card panel"><h3 style="margin-bottom:18px">Restaurante</h3><div class="stack"><label>Nome<input id="restaurantName" value="Restaurante do Pietro"></label><label>Moeda<select><option>BRL — Real brasileiro (R$)</option></select></label><button class="btn primary" id="saveSettings">Salvar alterações</button></div></div><div class="card panel"><h3 style="margin-bottom:18px">Preferências</h3><div class="stack"><label><input type="checkbox" checked style="width:auto;display:inline;margin-right:8px"> Alertas financeiros</label><label><input type="checkbox" checked style="width:auto;display:inline;margin-right:8px"> Mostrar dados de demonstração</label><div class="notice">Os dados deste protótipo são persistidos no navegador via localStorage.</div><button class="btn danger" id="clearData">Restaurar dados de demonstração</button></div></div></div>`}
function render(){const pages={dashboard,receitas:()=>tablePage('revenue'),despesas:()=>tablePage('expense'),fechamento:closing,analises:analytics,historico:history,calendario:calendar,relatorios:reports,configuracoes:settings};document.getElementById('content').innerHTML=pages[state.page]();document.getElementById('pageName').textContent={dashboard:'Dashboard',receitas:'Receitas',despesas:'Despesas',fechamento:'Fechamento do dia',analises:'Análises',historico:'Histórico',calendario:'Calendário',relatorios:'Relatórios',configuracoes:'Configurações'}[state.page];document.querySelectorAll('.nav-item').forEach(b=>b.classList.toggle('active',b.dataset.page===state.page));bind();charts()}
let chartsRef=[];function charts(){chartsRef.forEach(c=>c.destroy());chartsRef=[];if(state.page==='dashboard'){const dates=[...new Set(rows().map(x=>x.date))].sort();const labels=dates.map(formatDate);const rev=dates.map(d=>totals(rows().filter(x=>x.date===d)).revenue),exp=dates.map(d=>totals(rows().filter(x=>x.date===d)).expense),cats={};rows().filter(x=>x.type==='expense').forEach(x=>cats[x.category]=(cats[x.category]||0)+x.value);const rc={};rows().filter(x=>x.type==='revenue').forEach(x=>rc[x.category]=(rc[x.category]||0)+x.value);chartsRef.push(new Chart(document.getElementById('mainChart'),{type:'line',data:{labels,datasets:[{label:'Receitas',data:rev,tension:.35},{label:'Despesas',data:exp,tension:.35}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'bottom'}},scales:{y:{ticks:{callback:v=>'R$ '+v}}}}}),new Chart(document.getElementById('donutChart'),{type:'doughnut',data:{labels:Object.keys(cats),datasets:[{data:Object.values(cats)}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{position:'right',labels:{boxWidth:10,font:{size:10}}}}}}),new Chart(document.getElementById('revenueChart'),{type:'bar',data:{labels:Object.keys(rc),datasets:[{label:'Faturamento',data:Object.values(rc),borderRadius:5}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{ticks:{callback:v=>'R$ '+v}}}}}))}if(state.page==='analises'){const dates=[...new Set(state.data.rows.map(x=>x.date))].sort();chartsRef.push(new Chart(document.getElementById('profitChart'),{type:'line',data:{labels:dates.map(formatDate),datasets:[{label:'Lucro',data:dates.map(d=>{const t=totals(state.data.rows.filter(x=>x.date===d));return t.profit}),fill:true,tension:.35}]},options:{responsive:true,maintainAspectRatio:false,plugins:{legend:{display:false}},scales:{y:{ticks:{callback:v=>'R$ '+v}}}}}))}}
function bind(){document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>{state.page=b.dataset.page;document.getElementById('sidebar').classList.remove('open');render()});document.querySelectorAll('[data-period]').forEach(b=>b.onclick=()=>{state.period=b.dataset.period;render()});document.querySelectorAll('[data-add]').forEach(b=>b.onclick=()=>openModal(b.dataset.add));document.querySelectorAll('[data-delete]').forEach(b=>b.onclick=()=>del(b.dataset.delete));document.querySelectorAll('[data-edit]').forEach(b=>b.onclick=()=>openModal(state.data.rows.find(x=>x.id===b.dataset.edit)?.type,state.data.rows.find(x=>x.id===b.dataset.edit)));document.querySelectorAll('[data-close-date]').forEach(b=>b.onclick=()=>{state.page='fechamento';state.closeDate=b.dataset.closeDate;render()});const s=document.getElementById('tableSearch')||document.getElementById('historySearch');if(s)s.oninput=e=>{state.search=e.target.value;render()};const cat=document.getElementById('tableCategory');if(cat)cat.onchange=e=>{state.category=e.target.value;render()};const pay=document.getElementById('tablePayment');if(pay)pay.onchange=e=>{state.payment=e.target.value;render()};document.querySelectorAll('#tableSort').forEach(x=>x.onchange=e=>{state.sort=e.target.value;render()});const cd=document.getElementById('closeDate');if(cd)cd.onchange=e=>{state.closeDate=e.target.value;render()};const ex=document.getElementById('exportCsv');if(ex)ex.onclick=exportCsv;const clear=document.getElementById('clearData');if(clear)clear.onclick=()=>{localStorage.removeItem(KEY);state.data=load();toast('Dados de demonstração restaurados');render()};const saveSet=document.getElementById('saveSettings');if(saveSet)saveSet.onclick=()=>toast('Configurações salvas');}
function openModal(type,item=null){const isRev=type==='revenue';document.getElementById('modalEyebrow').textContent=isRev?'RECEITA':'DESPESA';document.getElementById('modalTitle').textContent=item?'Editar lançamento':isRev?'Adicionar faturamento':'Adicionar despesa';const cats=isRev?revenueCats:expenseCats;document.getElementById('modalBody').innerHTML=`<form id="entryForm" class="form-grid"><label>Data<input name="date" type="date" value="${item?.date||'2026-08-31'}" required></label><label>Descrição<input name="description" value="${esc(item?.description||'')}" placeholder="Ex.: Vendas do salão" required></label><label>Categoria<select name="category">${cats.map(c=>`<option ${item?.category===c?'selected':''}>${c}</option>`).join('')}</select></label><label>Valor (R$)<input name="value" type="number" step="0.01" min="0.01" value="${item?.value||''}" placeholder="0,00" required></label><label>Forma de pagamento<select name="payment">${payments.map(c=>`<option ${item?.payment===c?'selected':''}>${c}</option>`).join('')}</select></label><label>Observação<input name="note" value="${esc(item?.note||'')}" placeholder="Opcional"></label><div class="modal-actions wide"><button type="button" class="btn" data-close>Cancelar</button><button class="btn primary" type="submit">Salvar lançamento</button></div></form>`;document.getElementById('modal').classList.remove('hidden');document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeModal);document.getElementById('entryForm').onsubmit=e=>{e.preventDefault();const f=new FormData(e.target),obj={id:item?.id||crypto.randomUUID(),type,date:f.get('date'),description:f.get('description'),category:f.get('category'),value:Number(f.get('value')),payment:f.get('payment'),note:f.get('note')};if(item){const i=state.data.rows.findIndex(x=>x.id===item.id);state.data.rows[i]=obj}else state.data.rows.push(obj);state.data.demo=false;save();closeModal();toast(item?'Lançamento atualizado':'Lançamento adicionado');render()}}
function closeModal(){document.getElementById('modal').classList.add('hidden')};function del(id){if(!confirm('Excluir este lançamento?'))return;state.data.rows=state.data.rows.filter(x=>x.id!==id);save();toast('Lançamento excluído');render()}
function exportCsv(){const lines=[['Tipo','Data','Descrição','Categoria','Valor','Pagamento','Observação'],...state.data.rows.map(x=>[x.type,x.date,x.description,x.category,x.value.toFixed(2).replace('.',','),x.payment,x.note||''])];const blob=new Blob(['\ufeff'+lines.map(r=>r.map(v=>'"'+String(v).replaceAll('"','""')+'"').join(';')).join('\n')],{type:'text/csv;charset=utf-8'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='restoflow-financeiro.csv';a.click();URL.revokeObjectURL(a.href);toast('CSV exportado')}
function toast(s){const t=document.getElementById('toast');t.textContent=s;t.classList.add('show');setTimeout(()=>t.classList.remove('show'),2400)}
document.getElementById('quickMove').onclick=()=>openModal('revenue');document.getElementById('menuBtn').onclick=()=>document.getElementById('sidebar').classList.toggle('open');document.getElementById('logoutBtn').onclick=()=>document.getElementById('loginScreen').classList.remove('hidden');document.getElementById('loginForm').onsubmit=e=>{e.preventDefault();document.getElementById('loginScreen').classList.add('hidden');toast('Login realizado')};document.getElementById('demoLogin').onclick=()=>{document.getElementById('loginScreen').classList.add('hidden');toast('Modo demonstração ativado')};document.querySelectorAll('[data-close]').forEach(x=>x.onclick=closeModal);render();
